@model SmartUni.Models.TutorStatus
@using SmartUni.Resources
@inject Localizations SharedLocalizer

@{
    ViewData["Title"] = @SharedLocalizer.GetLocalizedHtmlString("Delete");
}

    <h2>@SharedLocalizer.GetLocalizedHtmlString("Delete")</h2>

    <h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this ?")</h3>
<div>
    <h4>@SharedLocalizer.GetLocalizedHtmlString("Tutor Status")</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TutorStatusDesc)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.TutorStatusDesc)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="TutorStatusId" />
        @if (Model.Tutor.Count >= 1)
        {
            <div class="alert alert-danger">
                <button type="button" class="close" data-dismiss="alert">
                    <i class="ace-icon fa fa-times"></i>
                </button>

                <strong>
                    <i class="ace-icon fa fa-times"></i>
                    Oh no!
                </strong>

                @if (Model.Tutor.Count == 1)
                {
                    <span>There's 1 tutor registered with this tutor status.</span>
                }
                else
                {
                    <span>There're @Model.Tutor.Count tutors registered with this tutor status.</span>
                }
                <br>
            </div>
        }
        else
        {
            <input type="submit" value="@SharedLocalizer.GetLocalizedHtmlString("Delete")" class="btn btn-default" />
        }
        <a asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
</div>
