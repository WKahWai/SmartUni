@model SmartUni.Models.StudyLevel
@using SmartUni.Resources
@inject Localizations SharedLocalizer

@{
    ViewData["Title"] = @SharedLocalizer.GetLocalizedHtmlString("Delete");
}

    <h2>@SharedLocalizer.GetLocalizedHtmlString("Delete")</h2>

    <h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this ?")</h3>
<div>
    <h4>@SharedLocalizer.GetLocalizedHtmlString("Study Level")</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StudyLevelDesc)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StudyLevelDesc)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="StudyLevelId" />
        @if (Model.Class.Count >= 1)
        {
            <div class="alert alert-danger">
                <button type="button" class="close" data-dismiss="alert">
                    <i class="ace-icon fa fa-times"></i>
                </button>

                <strong>
                    <i class="ace-icon fa fa-times"></i>
                    Oh no!
                </strong>

                @if (Model.Class.Count == 1)
                {
                    <span>There's 1 class registered with this study level.</span>
                }
                else
                {
                    <span>There're @Model.Class.Count classes registered with this study level.</span>
                }
                <br>
            </div>
        }
        else
        {
            <input type="submit" value="@SharedLocalizer.GetLocalizedHtmlString("Delete")" class="btn btn-default" />
        }
        <a asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
</div>
