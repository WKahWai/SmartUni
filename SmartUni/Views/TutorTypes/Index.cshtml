@model IEnumerable<SmartUni.Models.TutorType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TutorTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TutorTypeDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TutorTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TutorTypeDesc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TutorTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TutorTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TutorTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
