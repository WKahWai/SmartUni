@model IEnumerable<SmartUni.Models.TutorType>
@using SmartUni.Resources
@inject Localizations SharedLocalizer

@{
    ViewData["Title"] = @SharedLocalizer.GetLocalizedHtmlString("Tutor Type");
}

    <h2>@SharedLocalizer.GetLocalizedHtmlString("Tutor Type")</h2>

<p>
    <button class="btn btn-info" onclick="location.href='@Url.Action("Create", "TutorTypes")'">
        <i class="ace-icon fa glyphicon-plus bigger-110"></i>
        @SharedLocalizer.GetLocalizedHtmlString("Create New")
    </button>
</p>
<table id="simple-table" class="table  table-bordered table-hover col-md-6">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TutorTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TutorTypeDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @{ if (Model.Count() == 0)
            {
                <tr>
                            <td colspan="3" class="text-align-center">@SharedLocalizer.GetLocalizedHtmlString("-No data available -")</td>
                </tr>
            }
            else
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.TutorTypeId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TutorTypeDesc)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.TutorTypeId">@SharedLocalizer.GetLocalizedHtmlString("Edit")</a> |
                            <a asp-action="Details" asp-route-id="@item.TutorTypeId">@SharedLocalizer.GetLocalizedHtmlString("Details")</a> |
                            <a asp-action="Delete" asp-route-id="@item.TutorTypeId">@SharedLocalizer.GetLocalizedHtmlString("Delete")</a>
                        </td>
                    </tr>
                }
            }
        }
    </tbody>
</table>
